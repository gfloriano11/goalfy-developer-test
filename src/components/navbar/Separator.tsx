import styled from "styled-components";

function Separator(){
    const Separator = styled.span`
        font-size: 1.5rem;
        color: #E6E6E6;
    `

    return(
        <Separator>|</Separator>
    )
}

export default Separator;